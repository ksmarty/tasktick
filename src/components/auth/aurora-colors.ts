/**
 * Aurora stops for the auth headline.
 *
 * Drawn from GodUI's rainbow tokens rather than the registry's baked hex
 * spectrum: `globals.css` defines the rainbow as the one place this monochrome
 * palette lets colour in, so the headline opts into the theme instead of
 * carrying its own eight colours. The list is short so the sweep stays legible
 * across a three-word heading.
 */
export const AUTH_AURORA_COLORS = [
  'var(--rainbow-1)',
  'var(--rainbow-2)',
  'var(--rainbow-3)',
  'var(--rainbow-5)',
];
